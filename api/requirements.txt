# FastAPI Backend Dependencies (local development)
# NOTE: Render uses requirements-api.txt at the project root for deployment.
# Keep both files in sync.

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
supabase>=2.3.0
anthropic>=0.30.0
