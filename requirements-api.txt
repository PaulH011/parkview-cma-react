# FastAPI Backend + Calculation Engine Dependencies
# Used for Render deployment

# FastAPI server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Supabase (for dynamic defaults storage)
supabase>=2.3.0

# AI research (for quarterly assumption refresh)
anthropic>=0.30.0
