# The benchmark snapshot is regenerated by scripts/refresh_benchmarks.py, which
# always writes LF. Pin it to LF in the repo so the CI refresh never produces a
# line-ending-only diff (and thus no spurious pull request).
web/lib/benchmarks.json text eol=lf
